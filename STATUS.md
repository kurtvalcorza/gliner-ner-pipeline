# Release status

Current status: **Candidate** — E2E adaptation tutorial source complete, awaiting clean-runtime execution. The pipeline package, offline unit tests, one executed CPU smoke run from the two pinned snapshots, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/gliner_ner_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks are expected to run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
